// ============================================================
// Content Catalog — hardcoded seed data served by GET /content/catalog
// Replace with database queries when ready.
// ============================================================

// This is a minimal representation of the catalog metadata.
// The frontend VIRAL_CONTENT array is the source of truth for now;
// this file mirrors enough structure so the sync endpoint works.

module.exports = [
  // Placeholder: the frontend will fall back to its own VIRAL_CONTENT
  // until this file is populated from a real data source.
  // The GET /content/catalog endpoint returns this array.
];
